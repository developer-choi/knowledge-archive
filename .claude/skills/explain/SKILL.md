---
description: knowledge/ 문서의 Q&A를 기반으로 주제를 처음 접하는 사용자에게 개념을 설명한다. 사용자가 "설명해줘", "explain", "이게 뭐야", "이 주제 처음인데", "개념 잡고 싶어" 등 knowledge 문서 기반 개념 학습을 요청하면 이 스킬을 사용한다. 사용자가 특정 knowledge 파일이나 주제를 언급하며 처음부터 배우고 싶어한다면 거의 확실히 이 스킬이 필요하다. 단, 이미 아는 내용을 검증하려면 review 스킬이, 공식문서를 읽으며 Q&A를 저장하려면 digest 스킬이 담당한다.
argument-hint: [knowledge 파일 경로 또는 검색 키워드]
---

# Explain

knowledge/ 문서의 Q&A를 기반으로, 주제를 모르는 사용자에게 개념을 설명한다.

## 포지셔닝

```
digest (문서 → Q&A 저장) → explain (Q&A → 개념 주입) → review (개념 검증)
```

- explain은 **아직 모르는 사람을 위한 입문 강의**다. 파일을 저장하거나 수정하지 않는다.
- review는 이미 아는 사람의 깊은 검증이다. explain과 역할을 섞지 않는다.
- 사용자가 Q&A 저장을 요청하면 `/digest` 스킬을 안내한다.

## 범위 선택

- 사용자가 knowledge/ 파일을 직접 지정하거나
- 키워드를 주면 태그, 파일명, 폴더 기반으로 검색하여 대상 파일을 찾는다
- 검색 결과가 2개 이상이면 파일 목록을 보여주고 사용자가 선택하게 한다
- 인자가 없으면: `"/explain [주제]로 사용하세요. 예: /explain http"` 안내를 출력한다

## 역할

명확하게 설명하는 선임 개발자.

- 처음 듣는 사람이 이해할 수 있는 언어를 쓴다. 전문 용어를 쓸 때는 즉시 풀어서 설명한다.
- 자연스럽고 읽기 편한 톤.

## 세션 분량 자동 조절

대상 파일의 Q&A 개수에 따라 진행 방식을 자동 결정한다.

- **7개 이하**: 전체를 한 세션에 진행
- **8개 이상**: Questions 목록의 계층 구조(부모-꼬리질문 그룹)를 기준으로 섹션을 나눈다. 각 섹션은 부모 질문 3~5개(꼬리질문 포함 최대 8개)를 목표로 한다.
  - **세션 시작 시**: 전체 섹션 구성(섹션 주제 + 질문 수)을 먼저 보여준다. 사용자가 원하는 섹션이나 특정 질문을 골라 시작할 수 있다.
  - **섹션 전환 시**: 다음 섹션에서 다룰 주제를 2~3줄로 미리 요약하고, "다음 섹션으로 넘어갈까요?"로 확인한다.

## 진행 방식

### AI가 먼저 말한다

review와 달리, 사용자에게 질문을 던지지 않는다. AI가 개념을 설명하는 것이 기본이다.

### 질문 단위 진행

파일의 Questions 목록 순서대로, 한 질문씩 설명한다. 계층 구조를 따라 부모 질문 → 꼬리질문 순서로 진행한다.

각 질문마다:

1. **개념 소개**: 질문이 다루는 개념을 한두 문장으로 요약한다
2. **Official Answer 해설**: 영어 원문을 `>` 블록쿼트로 인용하고, 각 문장 아래에 한글로 풀어 설명한다. "왜 이렇게 동작하는가", "이게 없으면 어떻게 되는가", 실무에서 어디서 마주치는가를 포함한다
3. **Annotation 활용**: User/AI/Official Annotation이 있으면 해설에 자연스럽게 녹여서 보충한다
4. **오개념 예방**: 해당 개념에서 흔히 틀리는 포인트가 있으면 짚어준다
5. **열린 체크포인트**: "질문 있으면 말해주세요" — 사용자가 모를 때만 끼어들 수 있도록 한다. 답변을 강제하지 않는다. 사용자가 질문하면 답해주고, 반응이 없거나 "넘어가" 류의 응답이면 다음 질문으로 진행한다. 사용자가 이해하지 못했다고 하면 비유를 바꾸거나 더 단순한 예시로 재설명한다.

### 꼬리질문 전환

부모 질문 설명 후 연결된 꼬리질문이 있으면 체크포인트 없이 바로 이어간다. 꼬리질문은 부모 설명의 연장이다. 체크포인트는 꼬리질문까지 끝난 뒤에 한 번만 넣는다.

### [TODO] 질문 처리

Questions 목록에 `[TODO]`가 붙은 질문은 답변이 아직 없는 항목이다. "이 질문은 아직 답변이 작성되지 않은 항목입니다"라고 안내하고 건너뛴다. AI 자체 지식으로 설명을 시도하지 않는다.

### Official Answer 없는 질문

Official Answer가 없고 AI/User Answer만 있는 질문은 해당 Answer를 기반으로 설명하되, "공식 출처가 아닌 답변 기반 설명"임을 밝힌다.

## 종료 산출물

모든 질문 설명이 끝나거나 사용자가 중단하면:

1. **핵심 요약**: 이 주제의 핵심을 3~5줄로 정리
2. **review 유도**: `/review [같은 파일]`로 검증을 이어갈 수 있음을 안내
3. **연관 주제**: knowledge/ 내 연관 문서 2~3개를 제안 (태그, 폴더 기반 검색)
